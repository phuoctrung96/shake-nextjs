export type PaginationType = {
  diff: string;
  page_number: number;
  per_page: number;
  total_pages: number;
};

export type SuggestionPaginationType = {
  current_page: number;
  total_pages: number;
};
